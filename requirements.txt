# Direct runtime dependencies. Keep these exact pins and update them through a
# tested pull request, then refresh the production resolution in
# requirements.lock. Render installs only that lock; CI audits it.
Flask==3.1.3
SQLAlchemy==2.0.51
alembic==1.18.5
gunicorn==26.0.0

httpx==0.28.1

reportlab==4.5.1
python-dotenv==1.2.2
tzdata==2026.3

# Production integrations.
anthropic==0.117.0

# PostgreSQL driver selected by config.normalize_database_url().
psycopg[binary]==3.3.4
